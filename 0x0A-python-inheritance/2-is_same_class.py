#!/usr/bin/python3
"""A method that checks if object is instance of a class"""


def is_same_class(obj, a_class):
    """A testing function"""
    if type(obj) is a_class:
        return True
    else:
        return False
